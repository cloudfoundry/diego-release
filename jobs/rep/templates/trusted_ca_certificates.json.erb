<%=
  config = {}
  if_p("containers.trusted_ca_certificates") do |value|
    if value != nil and !value.kind_of?(Array)
      raise "container.trusted_ca_certificates needs to be either null or an array"
    end
    config[:trusted_ca_certificates] = value
  end
  config.to_json
%>
