base_job_templates:
  cc_bridge:
    - name: consul_agent
      release: cf
    - name: tps
      release: (( bridge_overrides.job_release || "cf" ))
    - name: cc_uploader
      release: (( bridge_overrides.job_release || "cf" ))
    - name: metron_agent
      release: cf
