set -e -x

export GOROOT=$(readlink -nf /var/vcap/packages/golang)
export PATH=$GOROOT/bin:$PATH

tar xzf etcd/etcd-UNSTABLE-20140205.tar.gz
pushd etcd
  ./build
popd

cp ./etcd/bin/etcd ${BOSH_INSTALL_TARGET}
