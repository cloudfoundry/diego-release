#!/bin/bash

set -e -x -u

STUBS_DIR=${PWD}/deployments-runtime/${DEPLOYMENT_NAME}/stubs

eval $(./scripts/ci/bosh_config)
./scripts/ci/bosh_setup

spiff merge templates/acceptance-tests-aws.yml ${STUBS_DIR}/aws.yml ${STUBS_DIR}/shared-secrets.yml ${STUBS_DIR}/ssl.yml > acceptance-tests-aws.yml
bosh deployment acceptance-tests-aws.yml
bosh -n deploy
bosh run errand diego_acceptance_tests
