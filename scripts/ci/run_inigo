#!/bin/bash

set -e -x


if which fly; then
  fly -c scripts/ci/run_inigo.build.yml -i src=src -i loggregator=loggregator -i scripts=scripts
else
  export DRONE_TMP=$TMPDIR/drone
  ./scripts/create_drone_yml scripts/ci/run_inigo.drone.yml .drone.yml
  drone -v -privileged build
fi
