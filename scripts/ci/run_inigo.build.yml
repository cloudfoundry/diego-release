
platform: linux

image: docker:///cloudfoundry/inigo-ci

inputs:
  - name: diego-release

params:
  GINKGO_PARALLEL: true
  DEFAULT_EVENTUALLY_TIMEOUT: 180s
  INIGO_REPEAT_COUNT: 1

run:
  path: diego-release/scripts/ci/run_inigo
